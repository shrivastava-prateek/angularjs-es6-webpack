import { Link, withRouter } from "react-router-dom";

function Navigation() {
  return (
    <div className="nav-link">
          <Link to="/react-app">
            React App
          </Link>
    </div>
  );
}
